[pytest]
testpaths = tests
asyncio_mode = auto
; One event loop for the whole run — the async DB engine and Redis client bind
; to the loop they first run on, so every test must share it.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
